apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-activity-deploymenty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-activity-servicey
  template:
    metadata:
      labels:
        app: user-activity-servicey
    spec:
      containers:
      - name: user-activity-servicey
        image: sachinitenn/github_user_activity_service:github_user_activity_service

---
apiVersion: v1
kind: Service
metadata:
  name: user-activity-servicey
spec:
  selector:
    app: user-activity-servicey
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  type: LoadBalancer
