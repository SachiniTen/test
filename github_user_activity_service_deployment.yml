apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-activity-deploymentnext
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-activity-servicenext
  template:
    metadata:
      labels:
        app: user-activity-servicenext
    spec:
      containers:
      - name: user-activity-servicenext
        image: sachinitenn/github_user_activity_service:github_user_activity_service

---
apiVersion: v1
kind: Service
metadata:
  name: user-activity-servicenext
spec:
  selector:
    app: user-activity-servicenext
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  type: LoadBalancer
