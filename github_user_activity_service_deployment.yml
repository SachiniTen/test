apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-activity-deploymentne
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-activity-servicene
  template:
    metadata:
      labels:
        app: user-activity-servicene
    spec:
      containers:
      - name: user-activity-servicene
        image: sachinitenn/github_user_activity_service:github_user_activity_service

---
apiVersion: v1
kind: Service
metadata:
  name: user-activity-servicene
spec:
  selector:
    app: user-activity-servicene
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  type: LoadBalancer
