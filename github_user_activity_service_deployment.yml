apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-activity-deploymento
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-activity-serviceo
  template:
    metadata:
      labels:
        app: user-activity-serviceo
    spec:
      containers:
      - name: user-activity-serviceo
        image: sachinitenn/github_user_activity_service:github_user_activity_service

---
apiVersion: v1
kind: Service
metadata:
  name: user-activity-serviceo
spec:
  selector:
    app: user-activity-serviceo
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  type: LoadBalancer
